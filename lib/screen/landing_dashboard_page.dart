import 'package:flutter/material.dart';

class LandingDashboardPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Container(
        child: Text('Selamat Datang'),
      ),
    ));
  }
}
